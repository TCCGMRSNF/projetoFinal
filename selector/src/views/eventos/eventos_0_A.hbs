{{>breadcrumb}}

<div class="div">
    <div class="h4 text-right">[{{evento.id}}] {{evento.descricao}}</div>
</div>
<br>

{{!-- Quesitos --}}
<div class="h5">  >> Quesitos</div>
<div class="table-wrapper-scroll-y my-custom-scrollbar">
    <table class="table table-striped table-sm table-hover mb-0">
        <thead>
            <tr>
                <th class="text-right">Id</th>
                <th>Descrição</th>
            </tr>
        </thead>

        {{#each quesitos}}
        <tr>
            <td class="text-right">{{id}}</td>
            <td class="text-left">{{descricao}}</td>
        </tr>
        {{/each}}
    </table>
</div>
<br>

{{!-- Avaliadores --}}
<div class="h5">  >> Avaliadores</div>
<div class="table-wrapper-scroll-y my-custom-scrollbar">
    <table class="table table-striped table-sm table-hover mb-0">
        <thead>
            <tr>
                <th class="text-right">Id</th>
                <th>Nome</th>
                <th>Email</th>
            </tr>
        </thead>

        {{#each avaliadores}}
        <tr>
            <td class="text-right">{{id}}</td>
            <td class="text-left">{{nome}}</td>
            <td class="text-left">{{email}}</td>
        </tr>
        {{/each}}
    </table>
</div>
<br>

{{!-- Candidatos --}}
<div class="h5">  >> Candidatos</div>
<div class="table-wrapper-scroll-y my-custom-scrollbar">
    <table class="table table-striped table-sm table-hover mb-0">
        <thead>
            <tr>
                <th class="text-right">Id</th>
                <th>Nome</th>
                <th>Email</th>
            </tr>
        </thead>

        {{#each candidatos}}
        <tr>
            <td class="text-right">{{id}}</td>
            <td class="text-left">{{nome}}</td>
            <td class="text-left">{{email}}</td>
        </tr>
        {{/each}}
    </table>
</div>
<br>