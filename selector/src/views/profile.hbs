{{>breadcrumb}}
<div class="container p-4">
    <div class="row">
        <div class="col-md-6 mx-auto">
            <div class="shadow-lg rounded card text-center text-white bg-info mb-3">
                <div class="card-body">
                    <h2>Bem-vindo ao Selector!<br></h2><h4>{{authUser.nome}}</h4>
                    <h5>{{authUser.email}}</h5>
                </div>
            </div>
        </div>
    </div>
</div>